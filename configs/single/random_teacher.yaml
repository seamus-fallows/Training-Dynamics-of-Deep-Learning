experiment:
  name: "random_teacher"

plot_history: false

model:
  in_dim: 5
  out_dim: 5
  num_hidden: 3
  hidden_dim: 10
  gamma: 1.5
  bias: false
  model_seed: 0

training:
  lr: 0.0005
  batch_size: null
  optimizer: "SGD"
  optimizer_params: null
  criterion: "MSELoss"
  batch_seed: 0

data:
  train_samples: 100
  test_samples: 100
  data_seed: 0
  online: false
  noise_std: 0.0
  params:
    matrix: "random_normal"
    mean: 0.0
    std: 1.0

metrics: []
      
max_steps: 2000
num_evaluations: 250
callbacks: []